_schema-version: 3.3.0
ID: customerorder
description: A simple CAP project.
version: 1.0.0
modules:
- name: customerorder-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: customerorder-db
  - name: uaa_customerorder
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: customerorder-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: customerorder-db
  parameters:
    buildpack: nodejs_buildpack
- name: customerorder-destination-content
  type: com.sap.application.content
  requires:
  - name: customerorder-destination-service
    parameters:
      content-target: true
  - name: customerorder_html_repo_host
    parameters:
      service-key:
        name: customerorder_html_repo_host-key
  - name: uaa_customerorder
    parameters:
      service-key:
        name: uaa_customerorder-key
  parameters:
    content:
      instance:
        destinations:
        - Name: approut_customerorder_html_repo_host
          ServiceInstanceName: customerorder-html5-app-host-service
          ServiceKeyName: customerorder_html_repo_host-key
          sap.cloud.service: approut
        - Authentication: OAuth2UserTokenExchange
          Name: approut_uaa_customerorder
          ServiceInstanceName: customerorder-xsuaa-service
          ServiceKeyName: uaa_customerorder-key
          sap.cloud.service: approut
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: customerorder-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: customerorder_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - sravanustsalesorderui.zip
      name: sravanustsalesorderui
      target-path: resources/
- name: sravanustsalesorderui
  type: html5
  path: app/salesorderui
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: customerorder-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: customerorder-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: customerorder-destination-service
    service-plan: lite
- name: customerorder_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: customerorder-html5-app-host-service
    service-plan: app-host
- name: uaa_customerorder
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: customerorder_xsuaa
    path: ./xs-security.json
    service: xsuaa
    service-name: customerorder-xsuaa-service
    service-plan: application
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
